azure.auth:
    clientId: testClientId
    tenantId: testTenantId
    clientSecret: testClientSecret
    skuType: TRIAL

azure.permissions:
    auth_clientId: testClientId
    auth_tenantId: testTenantId
    auth_clientSecret: testClientSecret
    resourceId: testResourceId
    roleAssignmentName: testRoleAssignmentName
    dpOwnerRoleDefinitionId: testRoleDefinitionId
    devGroupRoleDefinitionId: testRoleDefinitionId
    principalId: testPrincipalId
    principalType: testPrincipalType
    subscriptionId: testSubscriptionId
    resourceGroup: testResourceGroup

databricks:
    auth:
        accountId: testAccountId
    permissions:
        workload:
            owner: "CAN_MANAGE"     #CAN_EDIT, CAN_MANAGE, CAN_READ, CAN_RUN, NO_PERMISSIONS
            developer: "CAN_MANAGE"
        outputPort:
            owner: "SELECT"
            developer: "SELECT"

git:
    username: testUsername
    token: testToken
    provider: GITLAB

forkjoin:
    parallelism: 16

usecasetemplateid:
    workload:
        job: ["urn:dmb:utm:databricks-workload-job-template"]
        dlt: ["urn:dmb:utm:databricks-workload-dlt-template"]
        workflow: ["urn:dmb:utm:databricks-workload-workflow-template"]
    outputport: ["urn:dmb:utm:databricks-outputport-template"]

misc:
    developmentEnvironmentName: development
